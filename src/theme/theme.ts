/* ==========================================================
   SEYONIX THEME
========================================================== */

import {
  colors,
  typography,
  spacing,
  radius,
  shadows,
  gradients,
  glass,
  animation,
  breakpoints,
  components,
} from "../constants/design";

export const theme = {
  colors,

  typography,

  spacing,

  radius,

  shadows,

  gradients,

  glass,

  animation,

  breakpoints,

  components,
};

export default theme;